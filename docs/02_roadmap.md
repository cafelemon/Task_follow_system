# 公司工作任务跟踪系统版本规划

## 1. 文档用途

本文档用于记录公司工作任务跟踪系统的阶段规划、版本边界和优先级。每个阶段的具体实现应在进入开发前拆分为更细任务。

## 2. 规划原则

- 先保证任务跟踪主链路闭环，再做周报、日报、绩效等扩展集成。
- 先建设系统自己的数据模型、权限和历史记录，不把 Base 当作主业务系统。
- 先完成 Web 主操作入口，再逐步增强飞书卡片交互。
- 先满足管理层、负责人、执行人三个核心场景，再扩展更多统计和自动化。
- 每个版本都要有可验证的验收清单，不以“页面能打开”作为完成标准。

## 3. V0 文档和原型阶段

目标：把方向、边界、核心流程和验收口径定清楚，为开发做准备。

主要事项：

- 完成项目总览、产品需求、版本规划、架构、验收清单、AI 开发协议、进度和决策文档。
- 梳理现有 Base 字段、视图、自动化和周留存流程。
- 明确 MVP 数据对象、角色、权限关系和关键状态。
- 产出核心页面低保真或高保真草稿。
- 确认部署服务器、反向代理、NAS 附件路径和飞书应用准备情况。

交付标准：

- `docs/` 中 00 到 07 文档齐备。
- 核心版本范围和暂缓范围清晰。
- 关键业务口径已经记录在决策文档中。

## 4. V1 MVP 核心闭环

目标：完成任务创建、拆分、每周更新、时间线、权限和管理看板的基础可用版本。

版本号：`1.0.0`。

主要事项：

- 建立 FastAPI 后端服务、SQLAlchemy 数据库模型、Alembic 迁移机制和基础配置。
- 实现用户、部门、角色、权限矩阵的基础管理。
- 实现战略目标、母任务、部门任务、子任务、周更新、任务事件、通知记录等核心模型。
- 实现任务创建、任务拆分、周更新提交、任务详情时间线。
- 实现工作台、战略目标、母任务管理、部门任务、子任务执行、每周更新、会议看板、风险与逾期、历史时间线、通知记录、人员、角色权限、任务详情页面。
- 实现编号自动生成、人员选择、时间范围和必填字段校验。
- 实现自动会议看板和基础会议材料导出能力。
- 飞书 1.0.0 只做 open_id、模拟通知和卡片跳转预留，不接真实发送。
- 部署 1.0.0 采用本地优先 Docker Compose，附件默认本地目录，预留 NAS 配置。

交付标准：

- 执行人可以完成每周填报。
- 负责人可以拆分任务并查看执行人更新情况。
- 管理层可以查看整体状态、风险、延期和未更新情况。
- 任意任务可以查看历史进展。
- 权限控制能阻止无权限查看和编辑。

## 5. V1.1 飞书提醒和会议视图增强

目标：增强飞书入口和会议场景，让系统更贴近日常工作节奏。

主要事项：

- 接入企业自建应用机器人。
- 按用户身份推送执行人、任务负责人、管理层、信息中心卡片。
- 实现定时提醒、逾期提醒、未更新提醒和推送记录。
- 完善会议视图，集中展示本周重点任务、风险、延期、未更新、已完成和待决策事项。
- 支持生成会议版本或按周导出。
- 增加机器人推送失败、用户点击、处理结果等可观测记录。

交付标准：

- 用户可以从飞书卡片进入对应 Web 页面。
- 每周提醒能按身份准确发送。
- 管理层会议可以直接使用会议视图浏览重点信息。
- 信息中心可以查看推送和异常记录。

## 6. V1.2 页面层级和权限修订

目标：修正战略目标、母任务、部门任务三个页面的查看层级，让 Web 入口更贴近真实任务结构。

版本号：`1.2.0`。

主要事项：

- 顶部导航精简为姓名、部门、日期所属周和退出，移除快捷操作按钮和角色标签。
- 战略目标页四目标均匀展示，支持进入目标详情查看关联母任务。
- 母任务管理页增加页内目录，母任务详情展示部门级任务，不直接跳到子任务级别。
- 母任务管理入口限制为系统管理员、总经理、总经办秘书、观察者和母任务负责人。
- 部门任务页取消母任务卡片，直接展示部门级任务树状总览。

交付标准：

- 用户能按战略目标到母任务、母任务到部门级任务逐层钻取。
- 普通部门成员只能看到自己部门关联的部门级任务。
- 管理员和总经理可以通过部门侧栏查看不同部门任务。

## 7. V1.3 交互和周更新合并

目标：降低任务页面的误导性指标，把每周更新并入子任务执行，强化母任务管理操作。

版本号：`1.3.0`。

主要事项：

- 返回按钮统一放到页面标题左上方。
- 母任务管理目录点击直接进入母任务详情，新增母任务新增、编辑和归档入口。
- 全站移除进度条展示，不再用百分比表达任务推进。
- 每周更新入口并入子任务执行，子任务行内提供更新按钮。
- 周更新支持失焦自动保存草稿、提交保存和离开提醒。

交付标准：

- 母任务可按权限新增、编辑和归档隐藏。
- 子任务更新页可恢复草稿，提交后进入会议看板和历史链路。
- 旧每周更新菜单不再显示，旧路由跳转到子任务执行。

## 8. V1.4 部门任务编辑和子任务状态修订

目标：补齐部门级任务维护能力，并把子任务更新入口调整为更明确的执行状态流。

版本号：`1.4.0`。

主要事项：

- 母任务详情页支持新增、编辑和归档隐藏部门级任务。
- 部门任务总览将待拆解入口改为“拆解”，可直接新增有效子任务。
- 子任务执行页移除所属母任务列，只聚焦子任务、部门级任务和本周更新状态。
- 子任务更新页支持待开启、进行中、已更新、待更新和已完成状态流。
- 修复子任务更新页直达时空白的问题，增加加载、无权限和不存在提示。

交付标准：

- 部门级任务可按权限新增、编辑多负责部门、负责人和截止日期，并可归档隐藏。
- 待拆解部门任务新增子任务后，待拆解数量自动扣减。
- 新分配子任务必须先开启后才能填写更新，完成后锁定更新表单。

## 9. V1.5 会议看板和历史时间线修订

目标：把会议看板提升为第一入口，形成管理会议可直接使用的汇总视图，并把历史时间线改为按周展开的任务矩阵。

版本号：`1.5.0`。

主要事项：

- 取消工作台和风险与逾期独立菜单，会议看板作为系统第一入口。
- 会议看板拆分为总览、母任务看板和部门看板三个独立路由。
- 总览页提供指标卡、柱图、饼图、折线图、简版甘特图和风险逾期汇总。
- 历史时间线改为只读树状周矩阵，按母任务、部门级任务、子任务展开。
- 子任务新增启动时间，用于历史矩阵横向周列和后续状态联动。

交付标准：

- 登录和根路径默认进入会议看板总览。
- 管理层能从会议看板查看本周更新、风险、逾期和任务节奏。
- 历史时间线能按可读权限展示子任务周更新，不提供编辑入口。

## 10. V2 飞书真实接入阶段

目标：2.x.x 全部围绕飞书展开，把飞书从 1.x 的 open_id 和模拟记录预留升级为真实触达、身份入口、卡片回调、组织/人员辅助同步和后续飞书任务生态联动。Web 仍是复杂操作主界面，飞书是提醒、入口和轻量处理层。

### 10.1 V2.0.0 飞书出站消息第一版

版本号：`2.0.0`。

主要事项：

- 参考现有飞书桥接系统的 tenant token 缓存、企业应用配置和消息发送方式。
- 新增飞书企业自建应用配置：app_id、app_secret、API 地址、token 刷新提前量和请求超时。
- 新增飞书诊断接口，用于确认配置是否完整、tenant_access_token 是否可获取。
- 将通知记录从纯模拟升级为可真实发送：支持 `sent`、`failed`、`blocked`、`mock_sent` 等状态。
- 新增按子任务执行人 open_id 发送本周未更新提醒卡片，卡片按钮跳转 Web 周更新页。
- 未绑定 open_id、未启用飞书、配置缺失或接口异常时，必须在通知记录中保留明确阻塞/失败原因。

交付标准：

- 管理员可在通知记录页执行飞书诊断。
- 管理员可从通知记录页触发本周周更新飞书提醒。
- 已绑定 open_id 的执行人能收到周更新提醒卡片。
- 未绑定 open_id 的人员不会误发，通知记录显示 `blocked`。
- 飞书发送异常不会中断整批提醒，失败结果可追踪。

### 10.2 V2.0.1 飞书本地实测和手动绑定

版本号：`2.0.1`。

主要事项：

- 将本地测试环境配置为局域网访问地址，卡片按钮先跳转 `http://10.10.11.229:8080`。
- App ID/Secret 写入本地未跟踪 `.env`，不进入 Git 跟踪文件。
- 人员新增和编辑支持管理员手动录入 open_id。
- 新增测试卡片发送入口，用于先验证指定人员的飞书触达和 Web 跳转。
- open_id 保存前去除首尾空格，空字符串视为解绑，非空值必须全局唯一。

交付标准：

- 管理员能为自己的人员记录手动绑定 open_id。
- 飞书诊断能使用本地 `.env` 配置获取 tenant_access_token。
- 指定测试人能收到 2.0.1 测试卡片，并从卡片打开局域网系统地址。

### 10.3 V2.0.2 多人手动测试

版本号：`2.0.2`。

主要事项：

- 继续采用手动 open_id 绑定，为少量测试人员扩展真实提醒验证。
- 飞书卡片按钮改为服务端签名入口，验证通过后自动创建 session 并跳转业务页面。
- 签名链接包含目标用户、open_id、目标路径、过期时间和随机 nonce，不信任裸 URL 参数里的 open_id。
- 观察多人测试中的 open_id 冲突、权限边界、卡片打开率和提醒记录。
- 根据测试反馈调整卡片文案、按钮入口和通知记录展示。

交付标准：

- 刘星科可收到 2.0.2 测试卡片。
- 刘星科在无系统账号密码、无旧登录态的浏览器中点击卡片后可直接进入系统。
- 签名无效、过期、用户停用或 open_id 不匹配时不能创建 session。

### 10.4 V2.0.3 飞书免登绑定

版本号：`2.0.3`。

主要事项：

- 人员档案新增手机号字段，由管理员维护。
- 后台批量调用飞书通讯录接口，通过手机号解析并预绑定 open_id，解决未绑定人员无法被卡片触达的问题。
- 接入飞书网页免登入口，通过 code 换 user_access_token，再获取 open_id 创建系统 session。
- open_id 已绑定时直接登录；未绑定但手机号唯一匹配时绑定后登录。
- 手机号、姓名或 open_id 冲突时阻塞待人工处理，不自动覆盖已有绑定。

交付标准：

- 管理员可批量解析已录入手机号但未绑定 open_id 的人员。
- 已绑定 open_id 的人员可继续通过 2.0.2 签名卡片免密码进入。
- 用户可从登录页点击“飞书免登”进入系统。
- 冲突、未匹配、停用账号、state 过期或 code 无效时不能创建 session。

### 10.5 V2.0.4 邮箱预绑定、多人任务关系和生产内网端口

版本号：`2.0.4`。

主要事项：

- 删除手机号字段，改为人员邮箱字段。
- 支持上传包含 `姓名`、`邮箱` 的导出文件，按姓名匹配人员并写入邮箱。
- 通过飞书 `contact/v3/users/batch_get_id` 的 `emails` 参数批量解析 open_id。
- 母任务负责人、部门任务负责人、子任务负责人、子任务执行人支持多人。
- 多执行人子任务按执行人分别提交周更新、分别接收飞书提醒。
- Docker 生产内网入口端口通过 `TASK_FOLLOW_DOCKER_HTTP_PORT=28081` 指定，外网反代由 IT 配置。

交付标准：

- 邮箱导入冲突不自动覆盖，返回可读 blocked/conflict 明细。
- 多人负责人/执行人可搜索选择并以标签形式展示。
- 普通执行人只能提交自己的周更新，管理员可代填指定执行人的周更新。
- `/api/health` 返回 `version=2.0.4`。

### 10.6 V2.0.5 全局美化第二轮和任务人员关系刷新

版本号：`2.0.5`。

主要事项：

- 修复母任务负责人、部门任务负责人、子任务负责人和子任务执行人保存后页面显示不刷新的问题。
- 任务列表、详情页和子任务执行入口统一优先展示多人标签。
- 会议看板进行第一轮视觉美化，风格介于运营台和汇报屏之间。
- 风险与逾期汇总表固定列宽，长文本省略显示并支持悬停查看完整内容。
- 本地默认入口继续使用 `8080`；生产内网端口通过环境变量使用 `28081`。

交付标准：

- `/api/health` 返回 `version=2.0.5`。
- 修改负责人/执行人后，列表、详情和展开行立即显示最新多人关系。
- 会议看板总览、母任务看板、部门看板在桌面宽度下无明显文字溢出或列宽撑破。

### 10.7 V2.0.6 小屏适配与全局美化第三轮

版本号：`2.0.6`。

主要事项：

- 中小屏下侧栏自动收起为图标栏，保留导航能力。
- 页眉、内容区 padding、卡片间距在小屏下压缩，避免挤出屏幕。
- 部门任务、子任务执行、母任务详情展开表和会议看板汇总表收敛列宽。
- 长文本列省略显示，悬停查看完整内容。

交付标准：

- `/api/health` 返回 `version=2.0.6`。
- 约 1350px 宽窗口下 `/sub-tasks` 和 `/department-tasks` 不再整体横向溢出。
- 本轮不改权限、API、数据库结构和会议看板关联逻辑。

### 10.8 V2.0.7 核心业务页精修

版本号：`2.0.7`。

主要事项：

- 母任务管理、母任务详情、部门任务和子任务执行四个核心业务页统一视觉层级。
- 抽取轻量前端展示复用：任务标题、人员/部门标签、统计标签和表格标题区。
- 母任务卡片固定展示编号、标题、负责人、牵头部门、截止日期和统计信息。
- 业务表格、展开子表和子任务三组分区统一边界、悬停、标题和只读/更新操作样式。

交付标准：

- `/api/health` 返回 `version=2.0.7`。
- `/parent-tasks`、`/parent-tasks/:id`、`/department-tasks`、`/sub-tasks` 在桌面和中小屏下保持可读，不出现核心文字互相挤压。
- 本轮不改权限、API、数据库结构和会议看板业务关联逻辑。

### 10.9 V2.0.8 会议看板与全局 UI 收尾

版本号：`2.0.8`。

主要事项：

- 会议看板总览、母任务看板和部门看板统一指标卡、图表卡、tab 和汇总表视觉。
- 历史时间线保留矩阵结构，优化层级、表头、长文本省略和矩阵内部横向滚动。
- 通知记录页收敛为工具栏加业务表格，统一状态、结果和关联对象展示。
- 人员页统一新增表单、人员表格、邮箱和 open_id 绑定状态展示。

交付标准：

- `/api/health` 返回 `version=2.0.8`。
- `/meeting-board/overview`、`/meeting-board/parent`、`/meeting-board/department`、`/timeline`、`/notifications`、`/people` 在桌面和中小屏下保持可读。
- 本轮完成后 UI 美化阶段先冻结，转入生产迁移和真实使用反馈。

### 10.10 V2.0.9 看板关联性与周更新展开补强

版本号：`2.0.9`。

主要事项：

- 会议看板指标卡、本周更新柱图和风险占比饼图支持点击查看子任务数据详情。
- 任务甘特改为母任务截止日期管理，按月展示母任务编号，悬停显示母任务名称和截止信息。
- 母任务详情和部门任务总览的展开子任务表新增本周完成内容、遗留事项两列。
- 多执行人周更新按“姓名：内容”合并展示，未更新留空。

交付标准：

- `/api/health` 返回 `version=2.0.9`。
- 点击看板卡片、柱图、饼图、母任务截止日期图均能打开数据详情弹窗。
- `/parent-tasks/:id` 和 `/department-tasks` 展开子任务后可查看本周完成内容与遗留事项。

### 10.11 V2.1 飞书身份入口和绑定完善

主要事项：

- 完善 open_id + 姓名的账号绑定管理。
- 对比飞书返回身份和系统内人员档案，但部门、角色和任务权限仍以系统内配置为准。
- 增加绑定审计、解绑记录和离职/停用人员处理流程。

### 10.12 V2.2 飞书卡片回调和按身份卡片

主要事项：

- 接入飞书卡片回调验签、事件解析和幂等处理。
- 按执行人、任务负责人、管理层、信息中心推送不同卡片。
- 支持轻量确认、打开指定任务、跳转会议看板和记录点击。

### 10.13 V2.3 飞书数据生态联动

主要事项：

- 从现有 Base 或历史快照导入任务、子任务、周更新和附件索引。
- 建立历史数据清洗、字段映射和导入校验流程。
- 按需与日报、周报、飞书任务中心做单向或双向集成。
- 增强飞书推送、回调、点击、处理结果和异常的审计日志。
- 根据真实使用反馈优化飞书卡片效率和权限矩阵。

交付标准：

- 关键飞书触达链路可诊断、可审计、可重试。
- 用户能从飞书进入对应 Web 页面完成复杂操作。
- 飞书生态数据接入不破坏系统内任务模型和权限模型。
- 系统具备长期运维所需的飞书异常处理和审计能力。

### 10.14 V2.3.4 权限分层与总经办双层引导

- 区分部门负责人和任务负责人：部门负责人拆分牵头母任务，任务负责人拆分本人负责的部门任务。
- 子任务负责人强制继承部门任务负责人，导入、同步和 Web 操作使用同一规则。
- 引导身份固定为总经办会议相关、部门负责人、任务负责人、子任务执行者和观察者；技术管理员不形成业务引导。
- 引导分为首次进入系统的框架引导和首次主动点击左侧板块的专项引导。
- 2.3.4 先完成总经办系统框架和会议看板引导，后续版本每次完善一个角色。

### 10.15 V2.3.5 部门负责人双层引导

- 为部门负责人新增独立系统框架引导，不再使用通用短引导。
- 固定提供母任务管理和部门任务两个板块专项引导，首次主动点击左侧板块时触发。
- 本人存在有效子任务执行关系时，额外提供子任务执行专项引导，讲清兼任执行人的周更新责任。
- 本版只完善引导内容和触发机制，不调整权限、任务数据或角色分配。

### 10.16 V2.3.6 任务负责人双层引导

- 为任务负责人新增独立系统框架引导，不再使用通用短引导。
- 固定提供部门任务专项引导，围绕拆解子任务、指定执行人、继承负责人和跟踪进展展开。
- 本人存在有效负责或执行子任务时，额外提供子任务执行专项引导，区分“我负责”和“我执行”。
- 总经办和部门负责人仍按更高优先级展示对应引导；子任务执行者和观察者留待后续版本。

### 10.17 V2.3.7 子任务执行者双层引导

- 为子任务执行者新增独立系统框架引导，不再使用通用短引导。
- 本人存在有效执行子任务时提供“子任务执行”专项引导，围绕更新、开启、提交、风险和完成展开。
- 只有执行者角色但没有有效执行任务时，仅展示框架引导，不弹无数据的板块引导。
- 总经办、部门负责人和任务负责人仍按更高优先级展示对应引导；观察者留待后续版本。

### 10.18 V2.3.8 观察者双层引导

- 为观察者新增独立系统框架引导，不再使用通用短引导。
- 固定提供会议看板、母任务管理、部门任务和历史时间线四个只读审阅专项引导。
- 本人存在有效执行子任务时，额外提供子任务执行专项引导，提示其按执行人身份完成周更新。
- 总经办仍为最高优先级；带 `observer` 角色的用户优先展示观察者引导，避免多重身份用户被误判为任务负责人引导。

### 10.19 V2.4.0 正式上线前生产口径收敛

- 将 Web Base URL、飞书 OAuth callback、CORS、Cookie 和通知投递口径收敛到正式域名 `https://task.citronmicrobot.com:4442`。
- 生产通知投递切换为全员发送，生产默认隐藏并阻断测试卡片、四类验收卡片和模拟提醒入口。
- 新增生产配置模板和上线前预检脚本，检查正式域名、飞书权限、OAuth callback、open_id 绑定、调度、CORS、Cookie 和 Compose 端口。
- 2.4.0 只完成生产预备和风险暴露；3.0.0 才作为生产第一版发布标记。

### 10.20 V3.0.0 生产第一版

- 使用正式域名 `https://task.citronmicrobot.com:4442` 和反向代理对外提供服务。
- 飞书后台正式 redirect URL 已发布，通知全员口径可用。
- 生产数据库已从本机业务库迁移，人员、任务和周更新数据可读取。
- 生产迁移问题已沉淀到 `docs/09_production_deploy_runbook.md`，后续升级沿用离线镜像、数据库 dump、NAT 端口和预检指引。

### 10.21 V3.1.0 手机适配第一阶段

- 开启手机适配专项，先保障执行人在手机上从飞书链接进入系统、找到子任务、填写周更新和登记风险。
- 移动端采用顶部菜单按钮和抽屉导航，桌面端继续保留原侧栏布局。
- 子任务执行页在手机端从横向表格切换为分组卡片，优先展示“我执行”任务和更新入口。
- 子任务周更新页在手机端使用单列信息、纵向操作和底部保存/提交按钮。
- 本阶段不重做全部页面；会议看板、历史时间线、人员和通知等复杂页面后续分批适配。
- 3.x 中间版本不重新打生产离线包，下一次完整打包留到 4.0.0 前的测试闭合版本。

### 10.22 V3.1.1 手机适配第二阶段与撤回完成

- 手机端部门任务页改为顶部部门筛选、部门任务卡片和可展开子任务卡片，保障负责人拆解与跟踪链路。
- 手机端会议看板总览使用双列指标、紧凑图表、风险逾期卡片和全屏明细。
- 子任务负责人和管理员可将已完成子任务撤回为进行中；撤回后任务及当前周更新进度清零，历史内容和事件保留。
- 完成与撤回完成均增加确认提示，减少误操作。
- 母任务看板、部门看板、历史时间线和管理后台继续留待后续手机适配版本。

### 10.23 V3.2.0 生产通知收口与手机适配闭环

- 删除模拟提醒、测试卡片和四类验收卡片发送接口，正式通知代码不再存在验收数据与标题分支。
- 历史测试通知继续保留审计，但正式通知列表默认隐藏，可通过历史筛选查看。
- 完成母任务、两类会议汇总、历史时间线、通知、人员、权限和 Base 同步的手机专项布局。
- 正式通知、权限保存和 Base 清空导入增加二次确认，避免手机误触产生高影响操作。
- 3.x 中间版本仍不制作生产离线包，下一次完整打包留到 4.0.0。

### 10.24 V3.2.1 预检分级与周更新附件权限

- 生产预检只把真实业务通知目标缺 `open_id` 作为 blocker，暂无通知目标的通讯录人员降为 warning。
- 周更新支持上传、下载和删除附件，附件只关联具体周更新，不挂到上层任务。
- 附件下载权限跟随子任务/周更新可见范围：上传人、执行人、子任务负责人、部门任务负责人、总经办/观察者和管理员可按关系访问。
- 历史时间线只展示磁盘原文件存在的附件；历史缺原文件的附件元数据不再展示，从本周开始启用真实可下载附件。

### 10.25 V4.0.0 生产急修同步版

- 将已验收的手机适配闭环、生产通知收口、预检分级和周更新附件权限能力定格为生产同步版本。
- 重新制作生产离线包，包名、镜像包名、部署目录和健康检查版本统一为 `4.0.0`。
- 保持生产入口、反代、数据库 volume 和附件挂载策略不变，仅升级代码与镜像。
- 本版作为 3.x 生产运行后的第一次大版本同步，后续普通中间版本仍按“测试闭合后再打包”的节奏推进。

### 10.26 V4.1.0 部门管理

- 新增管理员部门管理页面，支持新增部门、编辑部门名称和删除无引用部门。
- 删除部门前检查人员、母任务、部门任务、多部门关联和子部门引用；存在引用时阻止删除并展示原因。
- 新增或改名后的部门继续通过现有 `/departments` 接口进入人员、母任务和部门任务选择器。
- 本版不默认制作生产离线包，验收后如需同步生产再按既有离线包流程执行。

### 10.27 V4.2.0 飞书汇报集成可行性与任务治理

- 本版只做方案验证，不开发新业务功能、不新增数据库表、不制作生产离线包。
- 明确产品方向为任务治理优先：临时或日常工作不能长期游离在任务树外，应进入“待归类事项”机制，最终补拆到任务树或关闭为非任务型记录。
- 明确技术边界：系统内生成个人周报可行；飞书原生汇报自动提交不进入本系统建设范围。
- 输出面向大领导、人事和系统建设侧的方案文档：`docs/10_report_integration_feasibility.md`。
- 后续建议：4.2.x 可做待归类事项最小原型，4.3.x 可做个人周报聚合页；飞书汇报回写不进入后续路线。

### 10.28 V4.2.1 工作台化待归类事项与任务治理

- 本版继续只做方案修订，不开发功能、不新增数据库表、不制作生产离线包。
- 将待归类事项升级为“双向奔赴”机制：员工从工作台提交事项和责任人，责任人判断挂树、补部门任务、提母任务诉求或关闭为非任务记录。
- 工作台重新定位为按角色组合的个人行动台；会议看板保留为总经办会议和管理审阅入口。
- 总经办秘书工作台应集中呈现母任务缺失诉求、部门任务缺失诉求、风险、逾期和未更新情况，支撑会议流程。
- 记录对 D-020 的修订：会议看板不再作为所有用户唯一首页，工作台成为个性化首页，会议看板成为会议审阅主入口。

### 10.29 V4.3.0 执行者工作台与待归类事项分版本规划

- 本版只做计划安排，不落地代码、不新增表、不制作生产离线包。
- 以 `4.2.1task.md` 为新增输入，将“待归类事项作为补充入口、周报中心作为聚合出口、工作台按角色组合展示”同步进方案。
- 保留正式任务主线：战略目标 -> 母任务 -> 部门任务 -> 子任务 -> 周更新 -> 历史记录 -> 会议看板。
- 新增辅助线：待归类事项 -> 部门任务补充/部门常态化/跨部门协作/周报补充 -> 周报中心 -> 周报材料生成。
- 4.3.x 聚焦执行者侧闭环：执行者进入工作台、看到本人任务、提交待归类事项、查看事项状态、形成个人周报材料和历史快照。
- 4.3.1 做执行者工作台框架，4.3.2 做待归类事项提交入口，4.3.3 做个人列表与状态可见，4.3.4 做周报中心草稿聚合，4.3.5 做周报确认、复制和历史快照，4.3.6 做执行者体验收尾与 4.4 准备。
- `4.2.1task.md` 中的数据对象设计只作为参考，最终字段以后端实现、权限边界和迁移风险为准。
- 责任人归类、任务负责人处理、部门负责人补部门任务、总经办秘书处理母任务缺失，统一留到 4.4.x 及后续版本。

### 10.30 V4.3.1 执行者工作台框架

- 本版落地执行者工作台框架，恢复工作台作为登录默认页；会议看板保留为管理审阅入口。
- 新增 `/workbench`，`/` 和旧 `/dashboard` 均跳转到工作台，左侧菜单第一项为“工作台”。
- 工作台复用现有 `/api/sub-tasks`，展示本人执行任务中的本周待更新、草稿未提交、临近截止、已完成和可登记风险事项。
- 前端新增页面独立放在 `frontend/src/pages/Workbench.tsx`，共用任务展示逻辑抽到 `frontend/src/ui/taskDisplay.tsx`，避免继续把新增页面堆入 `App.tsx`。
- 本版不新增数据库结构、不新增待归类事项提交能力、不恢复生产通知；4.3.2 再进入待归类事项底座。

### 10.31 V4.3.2 待归类事项提交入口

- 新增待归类事项数据底座和工作台提交入口，提交后状态为待确认。
- 归类方式固定为四类：挂载已有部门任务、本部门常态化工作、跨部门协作任务、周报补充记录。
- 选择“挂载已有部门任务”时，只能选择本人所属部门的部门任务；范围同时包含部门任务主部门和多部门关联。
- 4.3.2 不提供责任人同意/退回、双审核、转子任务、会议处理和个人列表，后续由 4.3.3 与 4.4.x 继续承接。

### 10.32 V4.3.3 待归类事项个人列表与状态可见

- 工作台新增“待归类事项”区域，包含“我的提交”和“待我确认”两个视图。
- “我的提交”展示本人提交事项、归类方式、关联对象、状态和提交时间，并允许提交人撤回 `pending` 事项。
- “待我确认”按潜在责任关系只读展示待确认事项：部门任务补充给任务负责人，部门常态化和跨部门协作给对应部门负责人。
- 本版不开放同意、退回、关闭、挂任务、双审核、部门统计和会议处理动作。

### 10.33 V4.3.4 周报中心草稿聚合

- 新增 `/weekly-report` 周报中心页面和 `GET /api/weekly-reports/draft` 实时草稿接口。
- 周报草稿仅返回当前登录用户自己的数据，聚合本人作为执行人的本周周更新、未撤回待归类事项、风险与卡点、下周计划。
- 正式任务进展和待归类事项分区展示，待归类事项不进入会议看板统计、不改变状态。
- 本版不新增 `weekly_reports` 持久化表，不做确认、复制、导出、历史快照、飞书原生汇报回写和通知；4.3.5 再进入确认快照。

### 10.34 V4.3.5 周报确认、复制与历史快照

- 新增 `weekly_reports` 快照表，通过 runtime schema 幂等建表，不需要整库重传。
- 新增确认、历史列表、详情和复制文本接口；周报快照只允许本人查看和复制。
- 周报文本固定六段：正式任务进展、跨部门协作补充、部门常态化工作、周报补充记录、风险与问题、下周计划。
- 周报中心支持确认当前周草稿、查看历史周报和复制飞书汇报可用纯文本。
- 本版不做导出文档、飞书原生汇报回写、通知、责任人同意/退回、挂任务、双审核、部门统计和会议处理。

### 10.35 V4.3.6 执行者体验收尾与 4.4 准备

- 4.3.6 不新增大功能，只做执行者侧体验收尾、移动端复核、空状态完善和 4.4 字段交接。
- 工作台、待归类事项和周报中心文案收口：待我确认本版只读，并为 4.4.0 责任人处理入口预留。
- 周报中心强化快照语义：历史快照不会随任务或事项变化自动改变，同一周再次确认会覆盖本周快照。
- 取消 `4.3.7` 至 `4.3.9` 缺陷缓冲规划；4.3.6 验收通过后直接进入 4.4.0 任务负责人/责任人处理线。

### 10.36 V4.4.0 待归类事项责任人处理线规划

- 本版只做落地规划，不改代码、不新增表、不制作生产离线包。
- 4.4.x 采用“能力主线 + 角色验收”，目标是打通 `4.2.1task.md` 明确的四类待归类事项处理流。
- 四类对象固定为：挂载已有部门任务、本部门常态化工作、跨部门协作任务、周报补充记录。
- 待归类事项未转为正式子任务前不进入会议看板正式任务统计。
- 4.4.1 做责任人处理底座：同意、退回、关闭和事件追溯。
- 4.4.2 做挂载已有部门任务处理：确认补充记录，必要时转成正式子任务。
- 4.4.3 做本部门常态化处理：部门负责人处理和追溯本部门常态化事项。
- 4.4.4 做跨部门协作处理：提交部门和协作部门负责人双确认。
- 4.4.5 原先延展的任务缺口上提在 4.5.3 已下线，历史字段仅作兼容保留。
- 4.4.6 做组合工作台复核：复核四类事项、周报中心和多身份模块叠加。

### 10.37 V4.4.1 责任人处理底座

- 本版开放工作台“待我确认”处理入口，责任人可对待归类事项执行同意、退回和关闭。
- 退回必须填写退回原因，关闭必须填写关闭说明；所有处理动作写入 `work_item_events`，提交人可在“我的提交”中看到最近处理记录。
- 部门任务补充和本部门常态化事项可同意为当前归类记录；同意不代表已转成正式任务。
- 跨部门协作事项本版不开放同意，只可退回或关闭，避免绕过 4.4.4 的双确认机制。
- 周报草稿继续展示 `pending` 和 `approved` 事项，过滤 `withdrawn`、`rejected` 和 `closed`；历史周报快照不被后续处理动作自动改写。
- 本版不做转正式子任务、跨部门双确认、通知恢复或生产离线包。

### 10.38 V4.4.2 挂载已有部门任务处理

- 本版只处理“挂载已有部门任务”类型待归类事项。
- 新增 `converted_to_sub_task` 状态，转成正式子任务后保存关联子任务和转换时间，防止重复转化。
- 部门任务负责人可将 `pending` 或 `approved` 事项转为正式子任务；负责人继承部门任务负责人，执行人由处理人选择，截止日期可空。
- “确认补充记录”继续使用 4.4.1 的同意动作；转子任务单独提供弹窗，要求填写子任务标题并选择执行人。
- 已转子任务事项不再进入待归类周报草稿，后续通过正式子任务周更新进入周报。
- 本版不做本部门常态化深化、跨部门双确认、通知恢复或生产离线包。

### 10.39 V4.4.3 本部门常态化工作处理

- 本版只处理“本部门常态化工作”类型待归类事项。
- 新增 `GET /api/work-items?scope=department-routine`，部门负责人查看本人所属部门的常态化事项记录，管理员和权限管理人员可兜底查看。
- 工作台“待归类事项”增加“本部门常态化”视图，展示提交人、状态、处理事件、提交时间和处理说明。
- 待确认常态化事项继续复用 4.4.1 的同意、退回和关闭动作；已处理事项只读展示。
- 已确认常态化事项进入提交人个人周报材料，但不进入正式任务树、会议看板任务统计或任务进度计算。
- 本版不做跨部门双确认、通知恢复或生产离线包。

### 10.40 V4.4.4 跨部门协作双确认

- 本版只处理“跨部门协作任务”类型待归类事项。
- `work_items` 增加提交部门确认和协作部门确认字段，用于保存确认人、确认时间和确认说明。
- 新增 `POST /api/work-items/{id}/cross-department-approve`，提交部门负责人和协作部门负责人分别确认；双方都确认后事项状态才变为 `approved`。
- 跨部门事项工作台卡片展示双确认进度；当前用户只看到自己有权确认的一侧。
- 任一方退回或关闭后流程结束，事项不进入后续周报草稿；已确认事项进入提交人个人周报材料和协作记录。
- 本版不做通知恢复或生产离线包。

### 10.41 V4.4.5 历史延展能力说明

- 该版本曾延展出任务缺口上提能力。
- 4.5.3 复核 `4.2.1task.md` 后确认，该能力不属于当前系统主线，已从公开接口和前端入口下线。
- 历史数据库字段和状态仅作兼容保留，不再作为后续路线依据。

### 10.42 V4.4.6 组合工作台复核与 4.5 交接

- 本版作为 4.4.x 待归类事项责任人处理线收尾版，不新增数据库表、不新增大功能、不恢复生产通知、不制作生产离线包。
- 工作台待归类事项区按多角色叠加展示“我的提交 / 待我处理 / 本部门常态化”，避免高优先级角色覆盖低层行动项。
- 待我处理收口为当前仍需处理的事项；已处理事项只读展示处理人、处理时间、说明和关联对象。
- 移动端复核工作台、待归类事项、周报中心和处理弹窗，补齐无提交、无待处理、无常态化记录和无历史周报空状态。
- 4.4.6 验收通过后直接进入 4.5.0，不安排 4.4.7 至 4.4.9。

### 10.43 V4.5.0 部门负责人工作台分版本规划

- 本版只做规划和文档同步，不改业务代码、不新增数据库结构、不制作生产离线包。
- 4.5.x 在四类待归类事项基础上，深化部门负责人工作台、部门统计和周报材料视图。
- 4.5.x 不新增缺口诉求、会议处理流或任务树补齐候选；超出 `4.2.1task.md` 的对象必须先确认。
- 4.5.1 做部门负责人工作台框架：展示本部门牵头母任务、本部门相关部门任务、待拆部门任务、未更新、风险、逾期和本部门常态化记录；本版只聚合展示和跳转。
- 4.5.2 做部门任务补充闭环：员工挂载已有部门任务的待归类事项，经负责人同意后成为部门任务补充记录，必要时仍可转为正式子任务；部门负责人工作台和部门任务视图均可追溯。
- 4.5.3 做路线回正与冗余下线：下线任务缺口和上提入口，文档回到 `4.2.1task.md`。
- 4.5.4 做部门统计与周报材料视图：只统计四类待归类事项，并在部门负责人工作台展示本周材料。
- 4.5.5 做周报中心输出体验增强：复制草稿、复制确认版本、统一文本格式；不做执行人导出，不做飞书原生汇报回写。
- 4.5.6 做 4.5 收尾与 4.6 交接：复核多身份叠加、移动端、空状态、权限边界；4.6.0 已将下一轮回正为工作台入口化与一屏化。
- 4.5.6 验收通过后进入 4.6.0；不安排 4.5.7 至 4.5.9，除非出现阻断级问题。

### 10.44 V4.5.1 部门负责人工作台框架

- 新增部门负责人工作台只读聚合接口 `GET /api/workbench/department-owner`。
- 部门负责人只能看到本人所属部门范围内的牵头母任务、相关部门任务、待拆部门任务、未更新子任务、风险/逾期和本部门常态化记录。
- 工作台新增“部门负责人”模块，按多身份叠加展示，不覆盖执行者、任务负责人、待归类事项和周报入口。
- 本版只提供卡片展示和跳转，不新增处理按钮，不创建任务，不关闭诉求，不写事件，不影响会议看板正式统计。
- 无部门的部门负责人返回空态；非部门负责人不展示模块。
- 4.5.2 进入部门任务补充闭环，不扩展任务缺口对象。

### 10.45 V4.5.2 部门任务补充闭环

- 按 `4.2.1task.md` 纠偏：4.5.2 主线改为“挂载已有部门任务”的补充记录闭环。
- 部门任务数据增加 `work_item_supplements`，部门任务展开区可查看待确认、已确认、已退回、已关闭和已转子任务的补充记录。
- 部门负责人工作台增加“部门任务补充记录”汇总和列表，统计范围仍限制在本人所属部门相关部门任务。
- 复用既有同意、退回、关闭和转子任务动作；本版不新增数据库表、不创建部门任务、不创建母任务、不恢复生产通知、不制作生产包。
- 记录纠偏决策：`4.2.1task.md` 的辅助线是待归类事项 -> 部门任务补充 / 部门常态化 / 跨部门协作 / 周报补充 -> 周报中心。

### 10.46 V4.5.3 路线回正与冗余能力下线

- 版本推进到 `4.5.3`。
- 下线此前延展出的任务缺口上提入口、相关 scope 和工作台展示，历史字段仅作兼容保留。
- 待归类事项公开能力回到四类对象：部门任务补充、本部门常态化、跨部门协作和周报补充。
- 部门负责人工作台移除任务缺口指标和分区，只展示任务推进、常态化、部门任务补充、未更新、风险和逾期。
- 后续新增对象或流程必须先单独确认，不能在实施中顺手扩展。

### 10.47 V4.5.4 部门统计与周报材料视图

- 版本推进到 `4.5.4`。
- 扩展 `GET /api/workbench/department-owner`，支持按 `week_key` 返回本部门本周周报材料统计。
- 统计范围只包含四类待归类事项：部门任务补充、本部门常态化、跨部门协作和周报补充。
- 跨部门协作在提交部门和协作部门双方部门负责人视图中可见，并区分本部门提交和协作到本部门。
- 部门负责人工作台新增“本周周报材料统计”分区，展示四类事项数量、状态分布和最近记录。
- 本版不新增数据库结构、不新增任务缺口对象、不写处理事件、不影响正式任务统计、不恢复生产通知、不制作生产离线包。

### 10.48 V4.5.5 周报中心输出增强

- 版本推进到 `4.5.5`。
- 周报中心支持复制当前草稿，复制草稿不会生成或覆盖历史快照。
- 已确认周报和历史周报复制统一使用新文本格式，老快照优先按快照内容重新生成文本。
- 周报文本格式按人员、周次、生成时间、正式任务进展、部门任务补充、跨部门协作、常态化、周报补充、风险卡点和下周计划组织。
- 手动复制弹窗改为多行文本区域，显示周次和字数，方便手机和浏览器剪贴板受限场景使用。
- 本版不做导出文档；观察者/人事导出需求已在 4.7.0 按月度 Excel 承接。
- 系统不做飞书原生汇报回写。

### 10.49 V4.5.6 4.5 收尾与 4.6 交接

- 版本推进到 `4.5.6`，作为 4.5.x 收尾版。
- 复核工作台多身份叠加展示：执行者任务、待归类事项、周报入口和部门负责人模块互不覆盖。
- 待归类事项只保留“我的提交 / 待我处理 / 本部门常态化”及部门任务补充、部门材料统计等 `4.2.1task.md` 明确入口。
- 周报中心收口为复制当前草稿、确认周报、复制已确认版本和历史快照，不做飞书原生汇报回写，不提供执行人导出文档入口。
- 部门负责人视图只读展示本部门任务、未更新、风险逾期、部门任务补充、本部门常态化和本周周报材料统计，不新增处理动作。
- 4.5.6 验收通过后进入 4.6.0；4.6.x 聚焦工作台入口化与一屏化，不继续扩展待归类事项业务对象。

### 10.50 V4.6.0 工作台入口化与一屏化规划

- 本版只做规划和文档同步，不改业务代码、不新增数据库结构、不制作生产离线包。
- 4.6.x 不做管理员运营工作台，也不做总经办工作台；工作台只保留执行者、任务负责人、部门负责人三个模块。
- 工作台定位调整为“一屏行动入口”：展示关键数量、最紧急 2-3 项和跳转按钮，详细信息回到各自业务页面处理。
- 当前工作台过长的主因是部门负责人完整明细、待归类事项完整面板和执行者多个任务列表直接铺在首页；4.6.x 将这些改成摘要卡片和入口。
- 4.6.1 做工作台一屏框架：顶部身份/周次区 + 三个角色摘要模块，无身份或无数据不铺长列表。
- 4.6.2 做执行者模块入口化：待更新、草稿未提交、临近截止、可登记风险四个关键数，最多 3 个紧急任务和三个入口按钮。
- 4.6.3 做任务负责人模块入口化：待处理待归类事项、可转子任务、本人负责部门任务、待拆子任务，最多 3 个待处理事项和跳转入口。
- 4.6.4 做部门负责人模块入口化：本部门相关部门任务、未更新、风险/逾期、部门任务补充、本周材料统计，最多 3 个重点异常或补充记录。
- 4.6.5 做视觉与手机收尾：复核 1440 桌面和 390/430 手机宽度，统一卡片密度、按钮文案、空状态和长文本截断。
- 管理员运营工作台暂缓到后续单独规划；总经办继续使用会议看板，不新增总经办工作台。

### 10.51 V4.6.1 工作台一屏框架

- 版本推进到 `4.6.1`。
- 工作台默认呈现改为顶部身份/周次区 + 执行者、任务负责人、部门负责人三张摘要卡。
- 三张摘要卡只展示关键数量、最多 3 条重点事项和跳转按钮；完整列表不再默认铺在工作台首页。
- 完整待归类事项处理面板和部门负责人详细面板保留在首屏下方折叠区，默认收起，避免功能丢失。
- 本版不新增后端写接口、不新增数据库结构、不恢复生产通知、不制作生产离线包。
- 4.6.2 至 4.6.4 继续分别深化执行者、任务负责人、部门负责人三张卡的字段与交互。

### 10.52 V4.6.2 执行者模块入口化

- 版本推进到 `4.6.2`。
- 将执行者卡从通用角色卡中拆为独立组件，工作台页面继续只负责编排和数据加载。
- 执行者卡固定展示待更新、草稿未提交、临近截止和可登记风险四个关键数。
- 重点任务最多展示 3 条，排序为草稿未提交、临近截止、待更新、可登记风险；同类按截止日期近到远。
- 每条重点任务展示子任务编号、任务名、所属部门任务、截止日期、本周状态和“更新/查看/风险”操作。
- 本版不新增接口、不改变周更新、风险、待归类事项或会议看板数据；任务负责人和部门负责人卡保持 4.6.1 状态。

### 10.53 V4.6.3 任务负责人模块入口化

- 版本推进到 `4.6.3`。
- 将任务负责人卡从通用角色卡中拆为独立组件，工作台页面继续只负责编排和数据加载。
- 任务负责人卡复用现有 `/work-items?scope=received`、`/department-tasks/overview` 和 `/sub-tasks` 数据，不新增后端接口。
- 任务负责人卡固定展示待处理事项、可转子任务、负责部门任务、待拆子任务四个关键数。
- 重点项最多展示 3 条，排序为可转子任务事项、其他待处理事项、可拆解部门任务、待跟进负责子任务。
- 本版不改变待归类事项、部门任务、子任务或会议看板数据；执行者卡保持 4.6.2 状态，部门负责人卡保持 4.6.1 状态。

### 10.54 V4.6.4 部门负责人模块入口化

- 版本推进到 `4.6.4`。
- 将部门负责人卡从通用角色卡中拆为独立组件，工作台页面继续只负责编排和数据加载。
- 部门负责人卡复用现有 `/workbench/department-owner` 数据，不新增后端接口。
- 部门负责人卡固定展示相关部门任务、未更新、风险/逾期、部门任务补充、本周材料五个关键数。
- 重点项最多展示 3 条，排序为风险/逾期、未更新、待拆部门任务、部门任务补充记录、本周材料异常。
- 本版不改变部门任务、风险、待归类事项或会议看板数据；执行者卡保持 4.6.2 状态，任务负责人卡保持 4.6.3 状态。

### 10.55 V4.6.5 工作台视觉与手机收尾

- 版本推进到 `4.6.5`。
- 统一三张角色卡的标题层级、指标块密度、重点项间距、按钮排列、空状态和长文本处理。
- 手机端强化工作台纵向布局、按钮全宽、状态标签换行和重点项文本截断，避免全局横向滚动。
- 折叠区文案收口为完整处理面板和部门负责人详细面板，明确详细信息不默认铺在首页。
- 本版不新增后端接口、不新增数据库结构、不恢复生产通知、不制作生产离线包。
- 4.6.x 工作台入口化闭合；管理员运营工作台和总经办工作台继续暂缓到后续单独规划。

### 10.56 V4.6.6 工作台三入口重排

- 版本推进到 `4.6.6`，作为 4.6.5 验收反馈后的布局修正。
- 工作台首屏从三角色卡修正为三类行动入口：待归类事项、子任务执行周更新、拆解入口。
- 待归类事项成为第一张独立卡，始终展示提交和处理入口；执行者卡移除待归类提交按钮，任务负责人卡聚焦拆解。
- 移除工作台标题下的小字说明和当前人员大卡，人员与周次压缩为轻量信息。
- 部门负责人详细能力保留下方折叠面板，不再占据首屏第三列。
- 执行和拆解入口的重点项收紧为最多 2 条，完整列表继续回到子任务执行、部门任务和待归类事项完整面板。
- 卡片网格按实际可见数量自动填满行宽：三张三列、两张两列、单张满行；手机端保持单列。
- 本版不新增后端接口、不新增数据库结构、不恢复生产通知、不制作生产离线包。

### 10.57 V4.7.0 观察者月度周报 Excel 导出

- 版本推进到 `4.7.0`。
- 新增 `GET /api/weekly-reports/monthly-export?month=YYYY-MM`，仅观察者、管理员或权限管理人员可导出。
- 导出按月份生成 Excel，周次列按该月内每个周一对应的 ISO 周动态生成。
- Excel 每个 active 用户占 4 行：任务线完成、归类项完成、下周计划、遗留事项；姓名列合并，冻结表头和前两列。
- 数据口径为已确认周报优先，未确认周次用实时草稿兜底并在单元格标记 `[未确认草稿]`。
- 工作台新增“观察者周报导出”轻量卡，仅 `observer` 角色可见；普通执行者、任务负责人和部门负责人工作台不受影响。
- 本版不新增数据库结构、不恢复生产通知、不制作生产离线包，不做飞书原生汇报回写。

### 10.58 V4.7.1 总经办使用指南更新与 App.tsx 拆分

- 版本推进到 `4.7.1`。
- 使用指南相关文案、角色 steps、菜单触发和 Tour 状态机拆到 `frontend/src/features/guides/`，`App.tsx` 只保留页面 refs、菜单触发、使用指南按钮和 Tour 渲染接入。
- 总经办/秘书系统框架引导更新为：工作台是进入系统后的行动入口，会议看板是会议审阅主入口。
- 会议看板专项引导更新为会议审阅顺序：总览异常、风险逾期、未更新、部门差异、下钻明细、会后跟进。
- `CURRENT_ONBOARDING_VERSION` 和所有角色 guide version 提升到 `2`；所有用户下一次进入系统都会重新看到一次对应指南。
- 其他角色指南本版只迁移不改写，后续按角色继续分批优化。
- 本版不新增数据库结构、不新增业务功能、不恢复生产通知、不制作生产离线包。

### 10.59 V4.7.2 部门负责人和任务负责人使用指南更新

- 版本推进到 `4.7.2`。
- 部门负责人指南更新为当前工作台三入口、部门负责人详细面板、部门任务补充、本部门常态化和本周材料统计口径。
- 任务负责人指南更新为拆解入口、待处理事项、可转子任务、部门任务补充处理和“不代执行人填报周更新”口径。
- 本版只修改 `departmentOwner` 和 `taskOwner` 两类指南，执行者、观察者、总经办和 legacy 指南不改写。
- 应用版本升到 4.7.2，但 `CURRENT_ONBOARDING_VERSION` 和所有 guide version 继续保持 `2`，已完成 v2 指南的用户不会再次自动弹出。
- 本版不新增数据库结构、不新增业务功能、不恢复生产通知、不制作生产离线包。

### 10.60 V4.7.3 执行者和观察者使用指南更新

- 版本推进到 `4.7.3`。
- 执行者指南更新为当前工作台“子任务执行周更新”入口、待归类事项、周报中心、附件、风险登记和完成撤回边界口径。
- 观察者指南更新为只读审阅、会议看板审阅顺序、任务追溯、历史证据和月度周报 Excel 导出口径。
- 本版只修改 `executor` 和 `observer` 两类指南，其他角色指南不改写。
- 应用版本升到 4.7.3，但 `CURRENT_ONBOARDING_VERSION` 和所有 guide version 继续保持 `2`，已完成 v2 指南的用户不会再次自动弹出。
- 本版不新增数据库结构、不新增业务功能、不恢复生产通知、不制作生产离线包。

### 10.61 V4.7.4 待归类事项自动审批与飞书通知

- 版本推进到 `4.7.4`。
- 新增 `work_item_automation_settings`，按 `user_id + category` 保存个人飞书通知和自动同意开关，默认全部关闭。
- 新增待归类事项审批与通知设置接口，并在工作台待归类事项卡和完整处理面板提供设置入口。
- 创建待归类事项后，系统按责任人个人设置执行自动同意和飞书卡片通知；生产通知仍受系统级通知配置、open_id 和白名单规则约束。
- 部门任务补充由关联部门任务负责人配置；本部门常态化和周报补充由提交人所属部门负责人配置；跨部门协作由提交部门和协作部门两侧分别配置，仍需两侧均自动或人工确认后才变为已确认。
- 自动同意、通知发送和撤销自动同意均写入 `work_item_events`，已自动同意事项可由触发责任人或管理员撤销回待确认。
- 本版不新增待归类事项业务对象，不自动转子任务，不创建任务，不改变正式任务统计，不恢复生产通知，不制作生产离线包。

### 10.62 V4.8.0 周更新快照安全回填

- 版本推进到 `4.8.0`。
- 新增一次性回填脚本 `scripts/backfill_weekly_updates_from_excel.py`，用于从 `2026公司工作任务跟踪表 (1).xlsx` 补齐 `2026-W24` 周更新。
- 回填只读取 `04_周更新进度` 中的本周完成内容、下周工作计划和遗留事项；`03_部门拆解任务` 只用于辅助匹配原始子任务编号。
- 写入策略固定为“只补空不覆盖”：同一子任务、同一执行人、同一周已有周更新时跳过并进入报告。
- 执行人严格匹配系统子任务执行人；匹配不到子任务或执行人的行只进入未写入清单，不做负责人兜底。
- 历史附件文件名不创建附件记录；脚本不改变子任务状态、进度、任务树和会议看板正式统计。
- 本版不新增数据库结构、不恢复生产通知、不制作生产离线包。

### 10.63 V4.8.1 历史周更新临时手工补录

- 版本推进到 `4.8.1`。
- 历史时间线临时增加“补录/编辑”入口，仅管理员和 `permission.manage` 可见。
- 新增临时接口 `POST /api/weekly-updates/manual-history-upsert`，按子任务、执行人和周次创建或覆盖周更新，保存状态固定为 `submitted`。
- 覆盖已有周更新前写入 `WeeklyUpdateRevision`，并写入 `manual_history_weekly_update_upsert` 任务事件，便于追溯。
- 本功能只用于修正 4.8.0 自动回填不完整或错误的数据；不导入历史附件、不发送飞书通知、不自动改变子任务当前状态。
- 下个版本删除该临时入口和接口，恢复历史周不可变口径。

### 10.64 V4.8.2 历史补录入口下线与工作台/时间线修正

- 版本推进到 `4.8.2`。
- 删除 4.8.1 临时接口 `POST /api/weekly-updates/manual-history-upsert` 和历史时间线补录/编辑入口。
- 历史时间线恢复只读展示，同时在桌面和手机端为每个子任务显式标注执行人。
- 工作台“拆解入口”收窄为进入部门任务的单一入口，待归类事项和子任务执行继续由各自入口卡承载。
- 已通过 4.8.1 写入的历史补录数据、修订记录和任务事件保留，不回滚、不清库。

### 10.65 V5.0.0 生产同步包与数据库快照

- 版本推进到 `5.0.0`。
- 本版作为 4.x 阶段生产同步包，包含工作台三入口、待归类事项、周报中心、观察者 Excel 导出、使用指南更新、自动审批通知设置和 4.8.x 历史周更新补齐结果。
- 生产离线包继续输出到 `release_packages/`，并按 5.0.0 版本只保留最新包。
- 打包脚本支持随包导出当前 PostgreSQL 数据库快照，路径为 `database/task_follow_5_0_0_prod_data_YYYYMMDD.dump`。
- 生产系统级通知配置恢复开启：调度启用、投递模式为 `all`、通知白名单为空；个人待归类事项通知开关继续默认关闭。
- 本版不恢复任何测试卡片、验收卡片或模拟提醒入口，不做飞书原生汇报回写。

### 10.66 V5.0.1 总经办部门任务全量查看修正

- 版本推进到 `5.0.1`。
- 修正部门任务页的目录切换权限：具备 `dashboard.view_all` 的总经办、秘书、观察者等用户可以在部门任务页查看全公司部门任务，不再被默认过滤回本人所属部门。
- 本次不扩大任务负责人、部门负责人、执行者的任务可见范围；他们仍按原有负责关系、部门负责人关系和执行关系查看。
- 明确 `dashboard.view_department` 是历史部门汇总权限名，不等同于部门任务页“查看本部门所有部门任务”的实际放行开关；部门任务页仍以任务可访问规则和全公司看板权限共同判断。

### 10.67 V5.0.2 部门任务权限与本部门汇总分组修正

- 版本推进到 `5.0.2`。
- 管理员、`permission.manage`、总经理/秘书管理口径、部门任务负责人和对应部门负责人可拆解部门任务到子任务。
- 任务负责人可查看本人所属部门的部门任务汇总，但只能拆解自己直接负责的部门任务；子任务执行者不因所属部门获得部门任务汇总。
- 部门任务页按“我负责 / 本部门任务汇总 / 全公司查看”分组展示，并使用后端返回的 `can_edit`、`can_split` 控制按钮。
- 工作台拆解入口只统计本人直接负责的部门任务，不因本部门汇总可见而扩大数量。

### 10.68 V5.0.3 执行者本部门任务只读查看

- 版本推进到 `5.0.3`。
- 执行者可在部门任务页查看本人所属部门的部门任务汇总，用于了解本部门重点工作。
- 执行者只读查看，不获得部门任务编辑、子任务拆解或代填周更新权限。
- 工作台保持现有三入口展示，不因执行者获得本部门汇总可见性而新增卡片或扩大拆解入口统计。

### 10.69 V5.0.4 工作台任务负责人入口个人化修正

- 版本推进到 `5.0.4`。
- 工作台“拆解入口”只统计当前用户作为部门任务负责人的任务。
- 用户同时具备管理员、总经理、观察者或其它全局查看身份时，不把全公司可见部门任务计入个人拆解入口。
- 部门任务页本部门汇总和全公司查看能力保持不变，完整查看仍回到部门任务页。

### 10.70 V5.1.0 手动周提醒与补充入口卡

- 版本推进到 `5.1.0`。
- 通知页手动周提醒一键发送两类正式飞书卡片：有待更新子任务的人收到周更新汇总卡，无待更新任务的 active 用户收到周报补充入口卡。
- 新增通知类型 `weekly_report_entry`，按钮跳转工作台，由待归类事项入口承接临时工作、常态化、跨部门协作和周报补充记录。
- 两类卡片按 `week_key + user_id` 交叉去重；同一周同一人收到任一周提醒卡后，重复点击只计入跳过。
- 每周五定时周更新提醒保持原有待更新子任务口径，不自动扩大到无任务人员补充入口卡。

## 11. 暂缓事项

以下事项不进入 V1，待核心模型稳定后再评估：

- 自动同步日报系统。
- 飞书任务中心深度双向同步。
- 复杂绩效考核和组织评分规则。
- 面向外部客户或跨公司协作的权限模型。
